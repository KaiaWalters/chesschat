import { createContext} from "react";
import { Box } from "@mui/system";

function PageContext() {
    return (
        <Box display={flex} flexDirection={column} sx={{p:3, border:'2px solid grey'}}>

        </Box> 
    )
}

export default PageContext